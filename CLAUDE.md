# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

Framework built (`retrodemos/framework/`). LED, LED II, Title, CD Player, and Bruce's Windows are all built (`retrodemos/demos/led.py`+`led_phases.py`, `led_ii.py`+`led_ii_phases.py`, `title.py`+`title_phases.py`, `cd_player.py`, `bruces_windows.py`). LED, LED II, and Title are each a scripted sequence rather than a single behaviour — see any of their phases for the pattern a multi-stage demo follows, and `framework/phase.py`/`graph_walk.py`/`ticker.py` for the machinery all three share. (Title started as a single continuous behaviour, then was rebuilt into the same 5-phase script as its siblings once that was confirmed as the right shape for it too.) Title needed its own renderer (`led_grid.BitColumnDisplay`) rather than reusing LED II's `DotMatrixDisplay` — its source image's pixel model turned out different enough (no bezel, no gap between columns, content computed from a byte value rather than a font) that forcing the reuse would have meant bending the abstraction, not applying it; see PLAN.md's "LED grid module" section before assuming a new demo fits an existing renderer. Because Title has two colour strips rather than one display, its phases drive both together through `TitleDisplays` (in `title.py`), not a single display object the way LED/LED II's phases do. Title's and LED II's own `SnakePhase`s were rebuilt (2026-08-24) into a snake-chase minigame — two `graph_walk.ChaseSnake`s hunting each other via a shared `graph_walk.ChasePair` — replacing a single wandering `Snake`; LED's segment-graph `SnakePhase` hasn't been ported yet. Dooley was built (2026-08-24), then cut from the project the same day on request — not going to work well as a demo; its demo code, tests, spec, and its `led_grid.BevelCellDisplay` renderer were all removed, and `DOOLEY1.png` is now listed under `demos.md`'s Excluded table. CD Player (2026-08-24) confirmed the "no shared grid/chrome reuse" prediction: it draws its own chrome directly, not through `led_grid.py`, and — like Dooley — isn't a `Phase`/`PhaseSequence` script, since simulated CD playback (a time counter, periodic pauses, a fake waveform) is one continuous loop rather than a scripted narrative. Bruce's Windows (2026-08-25) is the first interactive demo: clicking "Got it" needed `runtime.py`'s new mouse-coordinate rescaling (`_to_native_space`), since window-pixel mouse events had never needed converting to a demo's own native-canvas space before — every future interactive demo gets this for free (Cinqtris's click-to-slide interaction did, when it was built later). A same-day review found its chrome had real bugs (not just imprecision) and pushed it to byte-exact against `WINDOW1.png`; that work also settled the spec for the "Future: the unified desktop" end state (`PLAN.md`) — Bruce's Windows becomes the root interface of `retrodemos` itself, a 1024x576 icon-driven desktop, with the current exhibit becoming just one of the icons. Two foundational, demo-agnostic pieces for that: `framework/window_chrome.py` (the `bevel_rect`/`black_ring` primitives moved out of `bruces_windows.py`, plus a generic `render_window_chrome()` wrapper any demo's content can be opened inside) and `framework/pixel_font.py` (a hand-designed A-Z/0-9 pixel alphabet for arbitrary window titles, since `WINDOW1.png` only shows two fixed strings). `bruces_windows.py` itself dropped its own 320x240 canvas and drag logic back to a plain 200x200 static render now that dragging is the desktop shell's job. The desktop shell itself (`retrodemos/demos/desktop.py`, `DesktopDemo`) is built and is now the root interface: `python -m retrodemos` with no name opens it (1024x576, one icon per built demo — new pixel art, mocked up and confirmed with Bruce first), click an icon to open that demo as its own draggable/closable window; `python -m retrodemos <name>` still runs any one demo standalone. See `PLAN.md`'s "The unified desktop" section for the full mechanics; see `demos.md` for per-demo spec/build status. The desktop's icons were moved (2026-08-25) into a left-aligned grid (`ICON_GRID_ROWS`, column-major) instead of a single horizontal row, so they stay fully visible as more demos join. A pixel-accuracy pass on CD Player the same day (playtesting flagged it as "far from pixel perfect") went through three rounds, each catching something the last missed. First: box borders were rendered as two-tone bevels when the source uses flat single-tone grey outlines, and its digit font's "6 has no top bar, 9 has no bottom bar" quirks were fabricated — the source readout is an all-segments-lit test pattern, not a real per-digit calibration strip. Second (after being shown the source again): the whole top of the image is actually **two separate windows** side by side — a main player window (close button, "cd" logo, wide readout box with the dot-matrix spectrum living inside it, 5 transport buttons — not 6) and a taller companion equalizer window (own close button, own "cd" logo, 6 sliders — not 4) — not one merged panel the first pass had drawn; window frames now route through `framework/window_chrome.py`'s `bevel_rect`, its first caller besides Bruce's Windows. Third: the meter's "dots" are a genuine 2px diagonal glint, not a single pixel, and the two windows are truly separate and independently draggable in the source (clicking one reveals/reorders the other) — CD Player is now the project's second interactive demo, reusing `desktop.py`'s own per-window position/z-order pattern scoped to its two windows. A fourth round, this time playtesting the actual desktop shell, found three more real bugs: transport button icons were positioned with the wrong offset (up to 4px too far down, clipping the glyph); opening CD Player from the desktop nested its two hand-drawn windows inside a *third*, generic wrapper window (a window inside a window); and the equalizer showed by default instead of starting hidden. `desktop.py` gained a `chrome=False` mode for `_OpenWindow` so CD Player's own already-complete chrome doesn't get double-wrapped — its main and equalizer panels now open as genuine independent top-level desktop windows (`_open_cd_player_main`/`_reveal_cd_player_eq`), the first demo needing that; every other demo still goes through the generic single-window wrapper. Transport buttons also gained an invented press animation and working close buttons. All in `cd_player.py`/`desktop.py` (see their module docstrings and `docs/cd-player.md`); same connected-component-style re-verification method Bruce's Windows' bevel bugs were caught with. The same session added a macOS-style top menu bar to the desktop shell itself (new content, no source image): white, exactly one line of `pixel_font` text plus 2px padding on each side, a new ⌘ glyph at the left with a functional dropdown (About / Close All Windows / Quit), and the focused window's name after it, both rendered in the desktop's own teal with a black drop shadow for legibility against the white bar. Quit needed a genuinely new piece of framework — `Demo.want_quit`, a poll-based flag `runtime.py`'s loop now checks every frame, since previously only Esc/Q (handled before any demo saw the event) could end a run. Windows are clamped below the bar. A same-day follow-up swapped the drop shadow for a bold weight (`_draw_bold`, since a 1px-stroke font has no separate bold glyphs to switch to), made "HELP" itself clickable (opens a panel of condensed `README.md` content — `pixel_font` has no punctuation beyond an apostrophe, so it's paraphrased, not quoted), and changed how an open demo's icon behaves: instead of disappearing (playtesting: "that's weird"), it now stays visible and just dims, disabled rather than hidden — Bruce's Windows' icon is disabled outright regardless of open state, since a demo *of* windowing chrome reads as redundant on a desktop that's itself a real windowing system now. A further follow-up redrew the ⌘ glyph (a flat bracket pair read as "weird"; now four square loop-corners joined by a thin bridge, prototyped as several candidates rendered side by side before picking one — see `docs/pixel-archaeology.md`-style visual iteration) and fixed bold text running characters together: `_draw_bold` thickens each glyph a column to the right, which at the font's default 1px gap left no visible space at all, so `pixel_font.text_cells()` gained an optional `gap` parameter and the bar/dropdown/panels now lay out at `_BOLD_GAP` (2px) instead. 201 tests passing.

Build order 6 (Cinqtris, `retrodemos/demos/cinqtris.py`) is done. `CT_ANI.png` was another sprite sheet, not a screenshot of the finished screen — three animation strips (wordmark, equalizer, "MADMAX") stacked for storage, no layout to lift directly; `docs/cinqtris.md`'s original three-cells-across sketch didn't survive contact with the source. Design was worked out with Bruce over several rounds using a live HTML/canvas mockup (not a static PNG) before any Python was written — settled on a vertical stack (wordmark directly above the equalizer, both the same 128-native-px width and explicitly centred, since that's how they're sized in the source), the wordmark's source margin cropped from 2px to exactly 1px top/bottom (kept local to the wordmark only), and the equalizer's real sunken-bevel border restored after being briefly stripped by mistake. The spec's "10-segment equalizer" is actually 7 segments in the source — kept as measured. MADMAX's original spec (a button opening an About popup) was descoped in review to a click-anywhere slide animation instead, reusing the source's letter shapes laid out horizontally rather than its own stacked 2x3 grid. No `Phase`/`PhaseSequence`, same reasoning CD Player's continuous design used. Icon (a small equalizer-bars glyph) mocked up and confirmed before wiring into `desktop.py`. 210 tests passing.

Build order 7 (Bruce's 21, `retrodemos/demos/bruces_21.py`+`bruces_21_table.py`+`bruces_21_phases.py`) is done (2026-08-25). `CARDS.png` (672x264) reconstructed byte-exact as a 14x4 grid of 48x66 tiles — rows are the four suits, the first 13 columns are A-K, but the 14th column turned out to be non-card splash art (a "21"/MADMAX title card, a portrait, a "bruce's blackJack V.01" logo, a blank), left out of scope for now; `BACKS.png` (128x164) is a 2x2 grid of 64x82 tiles, two distinct back designs each duplicated top and bottom. This demo forced a real break from every prior one: instead of hand-encoding pixel data, it loads `CARDS.png`/`BACKS.png` at runtime (`pygame.image.load`+`subsurface`) and blits the real sprite pixels — confirmed with Bruce, since transcribing 52 detailed card faces by hand risked subtle errors for no benefit; `convert_alpha()` is skipped since `Deck` is built in `Demo.__init__`, before `runtime.run()` sets up a display surface, and plain unconverted surfaces blit fine at this sprite count. Built as a demo, not a game, per Bruce's explicit call: `AutoDealPhase` scripts a plausible-looking mock deal (dealer hole card, up-cards, a random 0-2 "hit", a reveal, maybe one more dealer card) but never computes a hand value or scores anything, matching the spec's own "visual only" note. Uses `Phase`/`PhaseSequence` like LED/LED II/Title (`DeckCyclePhase` shuffles all 52 cards plus both backs into a flip-through cycle; `AutoDealPhase` is the mock-deal script); `bruces_21_table.py` holds the `Deck` sprite loader and the shared `CardTable` display, split out from `bruces_21.py` itself purely to avoid an import cycle with `bruces_21_phases.py` — the same shape `led_grid.py` has relative to `led.py`/`led_phases.py`. Table layout (felt-green background, DEALER/PLAYER labels via `pixel_font`, fanned hands up to 5 cards) is new invented content, same situation Cinqtris's wordmark+equalizer layout was in. Icon (a card outline with a diamond pip) added to `desktop.py`'s `_DEMO_ENTRIES`. 223 tests passing.

Build order 8 (Tank Status Window, `retrodemos/demos/tank_status_window.py`+`_grid.py`+`_phases.py`) is done (2026-08-25) — the eighth and last of the originally planned demos. `WIN1.png` (273x350) reconstructed close to byte-exact: red/black outer frame, a title bar (flat minimize box, bevelled dropdown box), an 83x84 red/black dot-matrix grid, a smaller 83x9 secondary strip, and 11 blank grey buttons — measured, not guessed (the spec's own eyeballed "row of blank grey buttons" undercounted at ~8; a real pixel scan found 11). Dot geometry (2x2px on a 3px pitch) and on-colour (191, 0, 0) are pixel-identical to `led_grid.DOT_LIT`/`DotMatrixDisplay`'s own, but that class isn't reused directly — different bezel shape, and its `ROWS = 9` doesn't fit an 84-row grid; what does carry over: `led_grid.dot_grid_adjacency` for explosion topology and `framework/window_chrome.py`'s `bevel_rect`/`black_ring` for the button row and dropdown box. One real font surprise: `led_grid.DOT_FONT` matches the secondary strip's row count exactly but turned out to be digits/space/hyphen only (built for LED II's numeric marquee) — no letters — so `framework/pixel_font.py`'s A-Z alphabet went in instead for the status readout. The single biggest surprise: `WIN1.png`'s own grid is a lit-everywhere test pattern, not a captured game frame, so "tanks, walls, bullets, explosions" are entirely invented, scripted/looping content (the spec's own open question, already resolved in the spec in favour of the cheaper option) — three `Phase`s (`PatrolPhase`/`EngagePhase`/`ResetPhase`) over a shared `TankDisplay`, reusing `graph_walk.Burst` for every explosion (bullet impacts and the round-ending blast alike). Caught one real bug before shipping: `Burst.burned_out` requires `add_sparks()` to have been called once even with `spark_count=0`, and skipping it left bursts "expanding forever" from the caller's side, silently hanging `EngagePhase`/`ResetPhase` past their own end conditions — found by watching a full loop run longer than it should, not by a fast-passing test. Icon (a small tank-turret glyph) added to `desktop.py`. 236 tests passing — all 8 planned demos are now built.

## What this project is

Recreations of demo/UI programs Bruce originally wrote in the early 1990s for the Atari ST and early Windows machines. Each image in `images/` is a screenshot of one original program; most map to their own demo app, but a few closely related images share a single demo (see `demos.md` for the groupings). The demos are written in pygame-ce, and all of them share one CLI, one set of in-demo keybindings, and a common `Demo` interface, all defined in `retrodemos/framework/` (design rationale in `PLAN.md`).

## Commands

```bash
python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# List / run a demo
.venv/bin/python -m retrodemos --list
.venv/bin/python -m retrodemos <name> [--scale N] [--fps N] [--fullscreen]

# Tests (headless; SDL_VIDEODRIVER must be dummy, conftest.py sets it too)
SDL_VIDEODRIVER=dummy .venv/bin/python -m pytest tests/
SDL_VIDEODRIVER=dummy .venv/bin/python -m pytest tests/test_smoke.py::test_name  # single test
```

## Architecture

`retrodemos/__main__.py` is the single entry point for every demo (`python -m retrodemos <name>`). It owns argument parsing and discovers demos by scanning `retrodemos/demos/` for modules exposing a module-level `DEMO_CLASS` (a `Demo` subclass) — a new demo needs no launcher changes, just a module in that package.

`retrodemos/framework/runtime.py`'s `run()` drives every demo identically: it owns the window, the fixed-integer-scale canvas (`canvas.py`), and the shared keybindings (`keys.py`: Esc/Q quit, Space pause, R restart, consumed before a demo ever sees them). A demo only implements `handle_event` / `update` / `draw` / `reset` (`framework/demo.py`) — no demo touches `pygame.display` or the event loop directly. This split is also what makes `tests/test_smoke.py` possible headlessly: tests drive `run()` against a trivial in-test `Demo` with `max_frames` set, no real display needed.

Three demos (LED, LED II, Title) share `framework/led_grid.py`, but it turned out to hold three distinct renderer classes rather than one generic grid abstraction configured three ways — each demo's source image had its own pixel model closely enough that forcing a shared shape would have fought the source rather than matched it (see PLAN.md's "LED grid module" section for the full per-demo table and that history): `SevenSegmentDisplay` (LED: `render_raw` for per-segment control, `segment_adjacency` for the graph LED's snake/explosion phases path through), `DotMatrixDisplay` (LED II: `render_raw` for per-dot control, `text_dots` for its smoothly-scrolling marquee, `dot_grid_adjacency` for its snake/ripple phases, `DOT_FONT` for its 5x7 digit font), and `BitColumnDisplay` (Title: `render_values` for its byte-value-per-column content, `render_raw` for its snake/fireworks phases to address individual `(col, bit)` cells directly, no bezel, no gap between columns — a genuinely different pixel model, not a `DotMatrixDisplay` extension despite that being the original plan). `dot_grid_adjacency` is pure `(col, row)` topology with no dot-specific assumptions, so Title's bit grid reuses it directly (`dot_grid_adjacency(width, BitColumnDisplay.ROWS)`) rather than needing a near-duplicate "bit grid" function. (A fourth renderer, `BevelCellDisplay`, was built for Dooley on 2026-08-24 and removed the same day when Dooley was cut from the project — see PLAN.md's Status section.) Window chrome is deliberately *not* shared: Bruce's Windows, CD Player, and Tank Status Window each draw their own borders/title bars independently (rationale in `PLAN.md`'s "Window chrome" section).

A demo can be a single behaviour, or (like LED, LED II, and Title) a scripted sequence of phases that runs on a loop: `framework/phase.py`'s `Phase` base class (`update(dt) -> bool` returns True when finished, `draw(surface)`) plus `PhaseSequence`, which owns the phase list and index and runs it — advancing and calling `reset()` on the next phase when the current one finishes, looping back to the first phase after the last. A scripted demo builds its phase list in `__init__`, wraps it in a `PhaseSequence`, and delegates its own `update`/`draw`/`reset` to it (see `LedDemo`/`LedIIDemo`/`TitleDemo`); `retrodemos/demos/led_phases.py`, `led_ii_phases.py`, and `title_phases.py` have each demo's own `Phase` subclasses, the actual choreography. `Phase.display` doesn't have to be a single display object — Title's phases get `TitleDisplays` (in `title.py`), a small composite that drives both of its colour strips together, since Title's script is one script over two displays, not two independent ones. Reuse `Phase`/`PhaseSequence` as-is for any other demo whose spec describes multiple stages rather than one continuous behaviour — the sequencing itself needs no per-demo code (in practice, every demo built so far has ended up wanting one). A phase that advances in discrete steps (not continuously) should use `framework/ticker.py`'s `Ticker` for its dt-accumulation rather than hand-rolling one: LED's phases originally did this by hand and inconsistently, which is a live desync risk on a slow frame (see `Ticker`'s docstring). A phase that crawls or bursts across a grid (a snake, a radiating ripple/explosion) should use `framework/graph_walk.py`'s `Snake`/`bfs_rings`/`Burst` over that display's own adjacency graph (`segment_adjacency` or `dot_grid_adjacency`) rather than hand-rolling the walk — LED, LED II, and Title's snake/burst phases all share this now.

Extracting a demo's exact pixel font/grid from its source image is its own worked-out method — see `docs/pixel-archaeology.md` before starting that part of a new demo. The short version: don't hand-derive geometry by eye; find a repeated "clean" tile in the source, reconstruct the whole image from your rendering code, and diff it byte-for-byte against the source before trusting it.

## Planned workflow (from README priorities, in order)

1. Mini-spec per demo in `docs/<name>.md`. Done.
2. Shared interface design. Done — see `PLAN.md`.
3. Build and iterate on each demo, timeboxed to 1 day per demo, in the order in `PLAN.md`'s "Build order" table. Done — all 8 planned demos built (LED, LED II, Title, CD Player, Bruce's Windows, Cinqtris, Bruce's 21, Tank Status Window).
4. Polish pass on each demo, timeboxed to +1 day per demo.
5. The unified desktop (end state, not scheduled — see `PLAN.md`'s "Future: the unified desktop"): every demo eventually runs as its own window inside Bruce's Windows' desktop chrome, rather than staying a standalone `python -m retrodemos <name>` launch. Deferred until every demo above is built.
6. Add an `index.html` that highlights the demos, similar to `~/projects/peep --preview`'s format.
7. Document and publish to GitHub.
8. Write a retrospective/nostalgia post about the project.

Check `demos.md` for per-demo status (Spec/Build columns) before starting work on a specific demo.
